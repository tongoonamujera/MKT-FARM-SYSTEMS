json.extract! advertisement, :id, :Company_Name, :Date, :Item_Type, :item_Name, :Unit, :Unit_Price, :VAT, :Total_Amount, :user_id, :created_at, :updated_at
json.url advertisement_url(advertisement, format: :json)
