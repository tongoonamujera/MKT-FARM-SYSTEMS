json.extract! cashflow, :id, :Company_Name, :Date, :Season, :Month, :Grower_Name, :Hectrage, :Operation, :Unit_Price, :quantity_per_hectre, :Unity, :Input_Name, :Total_Amount, :Pro_Number, :user_id, :created_at, :updated_at
json.url cashflow_url(cashflow, format: :json)
